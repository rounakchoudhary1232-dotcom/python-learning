"use client";
import { useState } from "react";
import { ArrowUp, Mic, Paperclip, Sparkles } from "lucide-react";
import { GlassCard } from "@/components/ui/glass-card";
export function CommandBar() { const [message,setMessage]=useState(""); const [notice,setNotice]=useState(""); const submit=()=>{ if (!message.trim()) return; setNotice("Conversation requests will be available when the Phase 3 AI service is connected."); setMessage(""); }; return <div><GlassCard className="p-3"><div className="flex gap-3"><Sparkles className="mt-2.5 shrink-0 text-cyan-300" size={19}/><label className="sr-only" htmlFor="command">Ask JARVIS anything</label><textarea id="command" value={message} onChange={e=>setMessage(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();submit();}}} placeholder="Ask JARVIS anything..." rows={2} className="min-h-16 w-full resize-none bg-transparent pt-2 text-base text-slate-100 outline-none placeholder:text-slate-500"/></div><div className="flex items-center justify-between border-t border-white/[0.06] px-1 pt-3"><div className="flex gap-1"><button aria-label="Attach a file (unavailable)" onClick={()=>setNotice("File uploads are planned for a future phase.")} className="rounded-lg p-2 text-slate-400 hover:bg-white/[0.06] hover:text-cyan-200"><Paperclip size={18}/></button><button aria-label="Use voice mode (unavailable)" onClick={()=>setNotice("Voice interaction is planned for a future phase.")} className="rounded-lg p-2 text-slate-400 hover:bg-white/[0.06] hover:text-cyan-200"><Mic size={18}/></button><button onClick={()=>setNotice("Voice mode is planned for a future phase.")} className="ml-1 rounded-lg border border-white/[0.08] px-3 text-xs text-slate-400 hover:text-slate-100">Voice mode</button></div><button aria-label="Send request" onClick={submit} disabled={!message.trim()} className="grid size-9 place-items-center rounded-xl bg-cyan-300 text-slate-950 transition enabled:hover:bg-cyan-200 disabled:cursor-not-allowed disabled:opacity-35"><ArrowUp size={18} strokeWidth={2.4}/></button></div></GlassCard>{notice && <p role="status" className="mt-3 text-sm text-slate-400">{notice}</p>}</div>; }
